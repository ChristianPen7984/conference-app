import { ConferenceSearchPipe } from './conference-search.pipe';

describe('ConferenceSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new ConferenceSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
